# Terminal
`pwd`: See current directory.

`<name> --prefix`: See application installation path.

`open $(<name> --prefix)`: Open application folder.

`ls -la $(<name> --prefix)`: List all files, including hidden ones like `.git`.